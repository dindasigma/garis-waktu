export const eventAttributesMapping = {
    when: 'When',
    where: 'Where',
    what: 'What',
    which: 'Which',
};

export interface Event {
    when: string;
    where: string;
    what: string;
    which: string;
}